version: '3.8'

services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.10.2
    container_name: satria_mlflow
    ports:
      - "5000:5000"
    command: >
      mlflow server
      --backend-store-uri sqlite:////mlflow/mlflow.db
      --default-artifact-root /mlflow/artifacts
      --host 0.0.0.0
    volumes:
      - mlflow_data:/mlflow
    networks:
      - satria_network

  # Data Service
  data-service:
    build:
      context: ./services/data-service
    container_name: satria_data_service
    ports:
      - "8002:8002"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - SUPABASE_TABLE=${SUPABASE_TABLE:-water_quality_clean}
      - SUPABASE_PREDICTION_TABLE=${SUPABASE_PREDICTION_TABLE:-prediction_results}
    networks:
      - satria_network


  # ML Service
  ml-service:
    build:
      context: ./services/ml-service
    container_name: satria_ml_service
    ports:
      - "8001:8001"
    environment:
      - MLFLOW_TRACKING_URI=http://mlflow:5000
    networks:
      - satria_network
    depends_on:
      - mlflow

  # API Service (Gateway)
  api-service:
    build:
      context: ./services/api-service
    container_name: satria_api_service
    ports:
      - "8000:8000"
    environment:
      - ML_SERVICE_URL=http://ml-service:8001
      - DATA_SERVICE_URL=http://data-service:8002
      - SUPABASE_URL=${SUPABASE_URL:-}
      - SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY:-}
    networks:
      - satria_network
    depends_on:
      - ml-service
      - data-service

networks:
  satria_network:
    driver: bridge

volumes:
  mlflow_data:
